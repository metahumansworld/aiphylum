#!/usr/bin/env python3
"""chapman — the other peddler.

The hawker.py move, once more: this file imports peddler.py's act and adds
nothing, so the two guests at the fair are one program by construction,
differing in name and in the order the -guest flags are written on the
Makefile line — which is the order they see the board, and the order a tied
ask is broken in. Two readers of one book converged on the same number every
round on the arith cards, and the queue handed every one of those cards to
the front seat. This pair runs the same rule on the whole board, judged work
included, for a week:

    make fair-peddlers-7day

Whether the back seat is paid anything at all when the cards are the ones
the money is on is one of the things the trace is for.
"""

import aiphylum
from peddler import act

if __name__ == "__main__":
    aiphylum.run(act)
